# 雅思 AI 图文工作台使用与维护说明

**从一个真实卡点开始，把这一篇做完。**

雅思 AI 图文工作台是一个以本地使用为主的个人内容创作工具。它将选题、方法验证、结构梳理、拍摄准备、正文和修改拆成 16 个独立板块，让你打开就能继续，不必每次重新回忆整套流程。

你可以先记下一句话，等想做时再展开；也可以同时推进标题、配图和正文。文字与进度自动保存在当前浏览器，每篇笔记独立保留，并支持包含参考图的完整备份。

## 为什么做这个工作台

创作时，消耗精力的除了写作，还有反复找材料、想步骤、回忆上次做到哪里。这个工作台希望把这些动作变得简单，让更多精力留给真实尝试、判断和表达。

它服务于一个具体的创作方向：**用 AI 解决雅思学习中的真实卡点，记录过程，验证方法，再整理成图文。**内容保留个人语气与真实经历，不为了显得精致而补造过程或效果。验证没有帮助、结果暂时不确定，也可以如实写下来。

当前优先用于个人实践。先把流程用熟，通过真实创作发现问题，再逐步改进；未来可用实际案例向学员、朋友分享这套方法。

## 第一次使用：先留一个卡点

1. 用 Chrome 打开本目录的 `index.html`。已经配置桌面入口的电脑，也可以双击「雅思图文工作台」。日常填写无需安装开发依赖。
2. 在左侧展开 **「随手记一句」**，写下具体问题，例如：“这个单词看得懂，但录音里总听不出来。”
3. 点击 **「保存为草稿」**。系统用前 24 字命名，将原话放入第 05 步「真实卡点梳理」，其余字段留空。
4. 想开始做这篇时，点击 **「用这个卡点开始选题」**，原话会带入第 04 步；若已有选题，只跳转查看，不覆盖。
5. 按自己的节奏继续填写。看到保存成功的时间后，就可以关闭，下次从同一浏览器、同一路径继续。

随手记输入框里的文字需要点击保存才成为草稿。普通板块中的填写则会自动保存。参考图上传时，请等到保存结束。

## 16 个板块，一套可以跳着走的流程

| 阶段 | 板块 |
| --- | --- |
| 定位与选题 | 01 账号定位 · 02 内容原则与限制 · 03 账号人格摘要 · 04 选题 · 05 真实卡点梳理 · 06 五步初稿 |
| 结构与视觉 | 07 一句话概述 · 08 BAB 结构摘要 · 09 标题 · 10 开头 · 11 一页大纲 · 12 配图清单 · 13 找参考与拍摄 |
| 成文与打磨 | 14 正文 · 15 六轮修改法 · 16 导出成品 |

每步都有常见卡点、最低完成标准和可展开的提示。编号用于导航，可以自由切换；不必填完一整步才能保留草稿。

“未开始、进行中、待复核、已完成”帮助你辨认状态。关联内容变化后，对应板块会提示复核，已填文字和勾选仍然保留。修改后的内容需要重新确认完成。

## 帮你接着做的几个小功能

- **多篇笔记**：新建一篇不会清空旧篇，通过「我的笔记」切换。正常新建时可沿用前三项账号设定；随手记只保存卡点。
- **续写提醒**：自动记住上次所在板块，也可以选填一句“明天从第二个标题候选改”，再一键返回提醒对应的位置。
- **跨步引用**：标题与每张图的重点带入找参考任务；开头自动引用到正文，减少重复填写。
- **找参考与拍摄**：每次展开一张，按找、筛、按需生成、实拍四段推进；每段都有手动指引与可交给 Agent 的任务。
- **随时导出**：未完成也能导出 Markdown 草稿，全部完成并复核后可导出成品记录。

## 怎样与 AI 配合

点击 **「请AI帮我处理这一步」**，网页会整理当前任务和已有内容。将任务复制到 Codex，处理完成后再把结果填回对应板块。

第 13 步把第 12 步的整篇配图需求整理为一份任务，复制给 Workbuddy，只查本地参考库。选中参考后直接用自己的材料仿拍；本地不足需另行明确授权联网补搜。

目前网页没有直接调用 AI API，也没有自动回填、自动发布或多 AI 同时执行功能。填写、查看本地记录和导出可以离线进行；调用 AI 需要对应服务可用。

## 保存、备份与恢复

| 方式 | 包含什么 | 用来做什么 |
| --- | --- | --- |
| 浏览器自动保存 | 文字、进度、续写提醒；上传的参考图保存在浏览器素材库 | 同一设备、同一浏览器继续工作 |
| 「备份全部笔记」导出的 JSON | 全部笔记、进度和已上传的参考图 | 独立留底，或换设备后恢复 |
| 第 16 步导出的 Markdown | 当前笔记的文字、检查项、参考图文件名、来源链接和借鉴点 | 阅读、修改和整理成品；不用于完整恢复 |

自动保存会显示最近成功保存的时间。清除浏览器数据、更换浏览器或改变访问地址，可能无法读取原记录，建议收工或完成一篇后下载完整备份。

**恢复备份会添加独立副本，不覆盖或合并已有笔记。**恢复后优先打开备份时正在编辑的那篇；其他篇可在左上方「我的笔记」切换。重复导入会产生更多副本。目前没有自动云同步或定时备份，本地版应作为主要编辑入口。

保存失败时，页面会显示提示。请先保留当前内容并尝试备份，不要直接关闭。旧单篇记录会读入笔记库；可识别的旧六步记录会原样保留，不自动当作新步骤已完成。

## 素材和导出文件放在哪里

可在程序文件夹旁边维护「创作资料」，与程序代码分开：

```text
小红书/
├── content-workbench/          # 工作台程序
└── 创作资料/
    ├── 00-待整理素材/
    ├── 01-内容项目/
    │   └── 日期-选题关键词/     # 正式开始制作时再建
    │       ├── 01-原始素材/     # 实拍原片、真实截图、验证材料
    │       ├── 02-制作过程/     # 文稿草稿、AI 参考图、排版试稿
    │       └── 03-发布成品/     # 最终文案和图片
    └── 02-工作台备份/          # 完整 JSON 备份
```

这些是建议的资料目录，下载仓库不会自动创建它们。网页目前也不会自动归档文件或关联项目文件夹，导出后需要自行放入对应目录。随手记一个卡点时，不必同时创建项目文件夹。

## 开发与维护

程序由 `index.html`、`app.css` 和 `app.js` 组成。文字记录保存在 localStorage，参考图保存在 IndexedDB。GitHub 私有仓库只管理程序版本，不包含个人笔记、完整备份或上传素材。

准备开发环境需要 Node.js 和 npm：

```sh
npm ci
npx playwright install chromium
npm test
```

测试使用隔离浏览器，覆盖 16 个板块、跨步引用、并行填写、多篇笔记、保存与恢复、参考图、随手记转选题及移动布局，不操作日常浏览器中的笔记。

每次改动完成后，先检查和测试，更新 [更新记录](CHANGELOG.md)，再由使用者确认是否提交上传。确认后可执行：

```sh
npm run sync -- "本次改动说明"
```

命令依次测试、提交允许的程序文件并推送 GitHub；推送后再触发 GitHub Actions 检查。失败时不跳过检查、不强制推送。具体维护约定见 [AGENTS.md](AGENTS.md)。仅修改说明文档时，检查内容与链接即可，无需为了文案重复运行浏览器测试。

本地已有的旧原型和个人桌面启动器不随仓库上传。修改存储结构或回退程序前，先下载完整备份，并验证旧记录兼容性。

## 当前阶段

当前以本地桌面入口使用，代码保存在私有 GitHub 仓库，尚未部署 GitHub Pages。以后上线时，需要单独确认发布范围，并通过备份迁移数据；部署网页不会自动带来跨设备同步。

现阶段先用它完整做出真实内容，记录实际卡点，再决定下一项改动。工具应让创作更容易开始、更容易接续，也允许内容暂时停留在草稿。

## 三个 Skill，实际怎么用

不用先学会三个 Skill，才能开始填写。它们参与项目的方式不同，你只在需要的地方用就好。

**费曼视角，已经体现在第 04 步的提问里。**

打开「04 选题」，就能看到展开后的问题。比如刚需度对应「谁会在什么情况下被它卡住」，证据完整度对应「现在有什么证据，还缺什么」。照着回答即可，没有另外的启动按钮。原来的四个字段、已填内容和完成要求都保留。我们只借用了把术语讲具体、允许如实说明不确定性的思路，尚未添加全文自动评审。

**第 13 步：整篇一起找，一次交给 Workbuddy。**

1. 在第 12 步写下各张图要表达的重点。第 13 步会列出全部已填需求，不用逐张重新整理。只写好部分，也可以先一起找这部分。
2. 点「生成整篇找图任务（交给 Workbuddy）」，再点「复制任务」，发送给 Workbuddy。一个任务包含整篇需求、素材形式、已有账号背景与卡点。网页不会直接调用模型、读取本地参考库或启动搜索。
3. Workbuddy 只查 `~/WorkBuddy/小红书参考库/`，先读 `00_清单/小红书图文参考库_总表.csv` 或同名 XLSX，再实际查看 `01_图片/` 中的原图和已保存摘要。对每张已填的配图分别找 3—5 组有明显借鉴差异的真实参考，每组附主图；同一参考可服务不同配图，同一页不拿近似图凑数。本地不足三组就交实际找到的数量，逐项说明缺口。
4. 每张配图制作一页候选对照：主图并排展示，标明组号、本地编号、原图序号、文件位置和已有来源链接。每组分别说明「实际画面、适合用途、借鉴点」及与其他组的主要差异；该页推荐一组并说明为什么适合现有材料。旧标签只作线索，不能仅凭标题或批注猜画面含义。收到图片后，用自己的材料直接仿拍。

**这份任务不授权联网。** 库不存在、图片打不开、匹配不够，都应说明缺口，等你另行明确授权再补搜。不能自动去小红书搜索、打开在线原帖、请求 CDN、刷新赞藏或扩充库。已有链接只作为出处列出；这也不代表网页技术上能限制外部 Agent，需要 Workbuddy 遵守指令。

想留档时，展开「收到参考后，留存选中的图片或借鉴点（按需）」。在对应图下面保存本地编号 / 路径、原网页链接或上传图片，留一句借鉴点即可。也可以选择「沿用图 X 的参考」；被沿用的图需直接保存参考，不支持循环或连续沿用。已经有思路的图，选择「已有思路，无需参考」。五张图有重点且各有方向，仍可确认第 13 步完成，不要求先写完正文。

不再提供生成示意图的按钮。拍摄记录、自己的试拍和拍完勾选都在折叠区里，按需填写。想比较几张实拍时，展开「拍了几张，想请 Workbuddy 帮忙比较？」，生成一份整篇比较任务，并另外附上实际图片。**提示词里的文件名不会自动携带图片。**

上传支持 PNG、JPG/JPEG、WebP，每张不超过 12 MB；HEIC 请先转换。旧 A/B 图片、历史 AI 示意、旧候选和对照文字、拍摄与风格记录继续保留。历史示意只读查看、下载，不会被当成真实参考或生成入口。

完整 JSON 备份包含工作台已上传图片和各项记录，恢复添加副本；它不打包外部 Workbuddy 参考库。本地编号只是定位信息，不能代替图片备份。Markdown 记录文字、来源与文件名，不打包图片，也不会自动上传 GitHub 或归档到资料文件夹。

**产品评审，在真的遇到麻烦时请它帮忙看一眼。**

它叫 `utility-pm-critic`，由 Codex 在对话中调用，没有网页按钮。你可以告诉 Codex，在哪个板块、想完成什么、实际卡在哪里，再请这个 Skill 检查这一小段流程。它负责提出有理由、有具体改法的建议，是否采用由你决定。

第一次评审提出过第 13、14 步先后顺序的提示问题。讨论后用户选择保留原流程，本次建议未采纳，没有为此调整编号、按钮或跳转规则。

以后先用真实创作发现问题，再决定是否重新评审。Skill 用过了，不等于它的每个建议都要变成功能。具体试用状态留在 [项目记录](PROJECT.md)里。

## 固定备份文件夹

左侧展开「备份保存位置」，点击「选择备份文件夹」，选择自己已有的「创作资料 / 02-工作台备份」并允许访问。Chrome 授权后，点击「备份全部笔记」会优先直接写入这个目录，成功提示会显示目录和文件名。工作台会记住目录，但浏览器可能在下次使用时再次请求授权。

每份备份带时间和随机编号，保留多份，不覆盖上一份。没有设置目录、未授权或写入失败时，改用浏览器下载并明确提示；此时请自行把下载文件放进备份目录。可点「改回浏览器下载」清除目录设置，原文件不会删除。目录设置属于这台浏览器，不随 JSON 备份迁移。

这是点击备份后的保存位置设置，不是定时备份，也不影响其他网站的下载位置。首次目录选择须由使用者在浏览器中完成。
