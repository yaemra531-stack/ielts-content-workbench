# 雅思图文工作台维护规则

- 本目录是独立 Git 仓库。本地 index.html 的位置保持稳定，以保留桌面入口和浏览器记录。
- 本地版是日常主入口；GitHub 公开仓库保存程序版本，不保存用户笔记、备份 JSON、上传参考图或浏览器数据。
- 保留 16 个独立板块、并行填写和现有 UI 风格；按实际卡点做小改动。
- 修改数据结构时保持旧记录兼容；不得为测试清空用户实际浏览器的数据。
- 每次完成程序修改，运行 npm test；检查改动文件清单，更新 CHANGELOG.md，然后说明改动与测试结果，提醒用户确认本次是否提交并上传 GitHub。等待本次明确确认后再提交和推送；不沿用此前的自动上传授权。
- 用户确认本次提交上传后，可用 npm run sync -- "本次改动说明" 执行测试、提交与推送；未确认前不要运行该命令。失败需如实说明，不跳过检查、不强制推送。
- 未明确要求时不部署 Pages、不改变仓库可见性，不把数据备份称为自动同步。

- PROJECT.md 记录 Skill 试用、工具来源与后续待办。使用新 Skill 或完成试验后更新对应状态；区分“已使用”“已检查”“用户已确认”“待验证”，不把推荐或任务交接写成实际完成。
