# 项目记录

这里记三件事：我们实际用过什么、它帮我们改了哪里、接下来还想试什么。

日常用法看 [使用说明](USAGE.md)，已经发生的改动看 [更新记录](CHANGELOG.md)。本页由维护项目的 Codex 随工作更新，使用者不用为了记录再填一套表。

## 现在到哪里了

当前优先级是用工作台完整做一篇真实内容，熟悉流程，再根据实际卡点调整。保持本地使用，暂不扩展 API 连接、自动数据同步或素材管理系统。

| 事项 | 当前状态 | 可核对的结果 |
| --- | --- | --- |
| 16 板块、并行填写、多篇笔记、备份恢复 | 已实现并通过功能检查 | `app.js` 与三个 `test-*.cjs` |
| 随手记一句 → 选题 | 已上传，远端测试通过 | 提交 `59a0aaf`；已有选题不会被覆盖 |
| 给人看的 README | 已完成，用户认可；纳入本轮版本 | [README](README.md)；详细说明保留在 USAGE.md |
| 费曼视角改第 04 步 | 已完成，用户认可说明更具体；实际创作效果待验证 | 下方前后对照；是否更省力还需用户试填 |
| 第 13 步找参考与拍摄 | 已改为整篇一次匹配本地参考；本轮检查结果见文末，提交状态以 Git 记录为准 | 一份任务包含第 12 步所有已填需求，交给 Workbuddy 执行；真实筛选效果待用户验证 |
| 第一篇完整创作练习 | 待实践 | 功能测试不代表真实创作已跑通 |

这里的记录以可核对事实为准。以后上传新的改动时，同步更新状态，不把本地试改写成已上线。

## Skill 用在哪里

Skill 是给 Codex 的工作指引。这里列出的 Skill 不随网页加载，网页离线使用也不要求安装它们。它们不是页面中的自动 AI 功能。

| Skill | 使用状态 | 在本项目中的用途与证据 |
| --- | --- | --- |
| `khazix-writer` 卡兹克写作 | 已使用；用户认可方向，本轮再次明确要求使用 | 将 README 从功能手册改成从创作困扰出发的介绍；2026-09-22 用真实参考与生成示意的差异重写本次更新说明，详细操作留在 USAGE.md |
| `feynman-perspective` 费曼视角 | 已试用；用户认可，实际填写效果待验证 | 检查“会说术语”是否等于“知道该填什么”，只试改第 04 步 |
| `content-photo-reference` 拍摄参考 | 曾接入生图交接；现改为按需单独使用 | 用户实际反馈每张 A/B 和拍摄检查过重。第 13 步现优先查找真实参考，不默认调用生图 Skill；旧数据保留 |
| `utility-pm-critic` 产品评审 | 已试用；本次建议未采纳 | 提出第 13、14 步编号与依赖可能造成理解负担（P2）；用户讨论后决定保留原流程，未实施改动 |

本机 Skill 源文件（以安装目录为基准）：

- `khazix-writer/SKILL.md`
- `nuwa-skill/examples/feynman-perspective/SKILL.md`
- `content-photo-reference/SKILL.md`
- `utility-pm-critic/SKILL.md`

本页记录用途，不复制 Skill 全文。未核验的上游仓库与许可证不猜测，也不把所有 Skill 统称为开源项目。

## 第一次试验：不用先理解术语，也能开始选题

使用的是基于公开资料提炼的费曼思维框架，不是费曼本人对本项目的评价。

原来的第 04 步要求填写“刚需度、差异化、证据完整度”。它们有明确含义，但单看名称，第一次使用的人仍需要自己翻译一遍。

本次保持四个字段及其已有内容不变，只把说明写具体：

| 原标签 | 现在的问题 |
| --- | --- |
| 选题 | 这篇想解决什么问题？ |
| 刚需度 | 谁会在什么情况下被它卡住？ |
| 差异化 | 你能补充什么亲自尝试的过程或发现？ |
| 证据完整度 | 现在有什么证据，还缺什么？ |

原术语仍放在括号里，方便与之前的方法对应。提示明确允许写“尚未验证，准备怎样试”，避免把填完表格当成方法已经有效。

**怎样试：**打开第 04 步，用自己的一个真实卡点填一次。看看是否还需要问“这一格是什么意思”，以及写出的内容是否具体。先不扩展到全部板块。

**什么算有帮助：**用户能更直接地写出内容，并认为提示减少了犹豫。程序测试只能验证字段和流程没有坏，不能证明文案更好用。

## 第二次试验：让文稿变成拍摄参考任务

2026-09-20，用户确认第 13 步的交接方式，并授权上传本轮改动。功能检查已验证全文引用、封面仅两张、整套排除 AI 截图页、正文改动后参考图提示复核。尚未用一篇真实文稿完成生图与实拍，不把交接成功记成拍摄效果验证成功。

当时的操作顺序是（已由下方 2026-09-21 重构替代）：先写第 09 步标题、第 10 步开头和第 14 步正文 → 回第 13 步点“先试封面 A/B” → 复制任务到 Codex → 生成两张参考图 → 将图片放回图 1 的 A/B 位置 → 对照实拍，拍完再勾选。

旧版拍摄准备依赖正文，交接并不直接生成或回填图片。新版找参考取消正文前置要求。

## 第三次试验：评审建议可以不采纳

2026-09-20，在 Codex 对话中以内联方式执行 utility-pm-critic，对使用说明和既有操作路径做了一次简短评审。参考了上游评审规范与验收标准，提出一项 P2 建议：让下一步提示更明确反映第 13 步依赖第 14 步正文的关系。

用户说明原流程来自此前已确认的材料，讨论后选择保持原样。本次评审已完成，但建议未实施；没有改变编号、按钮或跳转逻辑，也不把这个建议登记成必须完成的待办。

后续使用原则是先有实际问题，再针对问题评审。Skill 提供参考，由使用者决定是否修改。

## 接下来的实际验证

| 待办 | 什么时候开始 | 最小试验 | 做完看什么 |
| --- | --- | --- | --- |
| 找参考后完成自己的试拍 | 本地新版试用时 | 从第 12 步选一张，走过找参考与筛参考；按需生成示意，再用自己的材料试拍一张 | 是否借到了核心特质、能否用现有条件实现、是否更容易开始 |
| 产品评审再次使用（按需） | 实际创作出现具体麻烦时再启动 | 提供操作目的和实际卡点，只评审这一段流程 | 建议是否解决真实问题；用户决定采用或保留现状 |

费曼视角已做说明微改；第 13 步已进行一次真实找图、生成示意与对照分析，四段页面已被下方整篇本地匹配版本替代；新的匹配与实拍效果待真实使用验证。产品评审已完成首次试用，用户未采纳本次建议；以后按实际卡点决定是否再用。没有文稿或真实使用反馈时，待办可以一直留着，不需要为了打勾造材料。

## 实际用了哪些工具

| 工具或技术 | 在这里做什么 | 关系与来源 |
| --- | --- | --- |
| HTML、CSS、JavaScript | 构成页面和交互 | 浏览器原生技术；页面没有前端框架或运行时 npm 依赖 |
| localStorage、IndexedDB | 保存笔记文字、进度与参考图 | 浏览器存储能力；不是云端数据库 |
| Playwright 1.62.1 | 在隔离浏览器检查保存、流程和图片 | 开发测试依赖；Apache-2.0，版本与许可已从安装包核验；[项目仓库](https://github.com/microsoft/playwright) |
| Node.js 与 npm | 安装测试依赖、执行维护命令 | 开发环境，日常打开网页不需要；[Node.js](https://nodejs.org/)、[npm CLI](https://github.com/npm/cli) |
| Git | 保存代码历史、提交改动 | 版本管理工具；[官方网站](https://git-scm.com/) |
| GitHub 与 GitHub Actions | 私有代码托管、上传后自动检查 | 托管服务，不把服务本身列为开源依赖；当前未开启 Pages |
| Codex | 修改程序、整理 AI 任务、协助内容创作 | 外部服务，不内置在工作台，也不由页面自动调用 |

新加工具时，只有实际参与过本项目才放进这张表；候选留在待办。若分发第三方源码或素材，再核对其授权和署名要求。

## 参考成熟项目的做法

我们先用三个入口，各自只管一件事：README 负责介绍，PROJECT.md 负责试验和待办，CHANGELOG.md 负责记录已经发生的改动。

- [GitHub Issues / Projects](https://docs.github.com/en/issues/planning-and-tracking-with-projects/learning-about-projects/about-projects) 能把待办呈现为表格、看板和路线图。将来多人参与或待办明显变多时，再迁移过去。迁移后以那里为准，避免两边重复维护状态。
- [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) 提倡为人整理重要变更。我们借用“待发布 / 已发布”的区分，不直接拿 Git 提交列表替代使用者能读懂的更新说明。

当前不嵌入远程看板，也不为此增加工作台必填项。本页可以离线阅读，随本轮代码一并纳入私有仓库。

## 2026-09-21 · 恢复备份后看起来内容缺失

用户实际反馈恢复后很多内容看不到。隔离浏览器复现发现，程序固定打开备份列表第一篇；当第一篇为空白、当前编辑的是第二篇时，会产生内容丢失的错觉。逐字段比对确认此次备份的已填写内容完整恢复。

本地修复为优先打开备份里的当前笔记，无法匹配时再回退第一篇，并在恢复提示中说明当前打开哪篇、如何切换其他笔记。保留原有恢复副本策略，不覆盖用户记录。已用用户备份在隔离浏览器比对字段，并新增从第二篇实际导出、恢复后检查当前板块与编辑状态的回归用例。用户确认这是预期行为，并授权本轮上传；实际浏览器使用效果仍待确认。

## 固定备份目录（实际目录授权仍由使用者完成）

新增浏览器目录选择与记忆，用户首次选择已有备份目录并授权后，备份按钮优先写入该目录。默认路径建议为「创作资料 / 02-工作台备份」。授权被拒绝或写入失败时明确回退浏览器下载。隔离测试已验证目录写入、刷新后记忆、历史文件不覆盖、拒绝授权及写入失败时回退下载。目录句柄测试采用浏览器测试目录，实际用户目录尚未选择，不能视为已经配置完成。

## 2026-09-21 · 第 13 步从拍摄检查改为找参考（用户已确认）

用户指出自己缺少可借鉴的画面，当前更需要找到适合自己、能实现的参考，明确授权重构。原 A/B 必填、四项风格确认、拍摄完成和正文前置要求取消。保持 16 步与原有 UI 风格，每次展开一张配图；使用图片或网页链接加借鉴点，也可沿用另一张已选参考或标记已有思路。

本次只实现需求交接与参考保存，未执行真实搜索，未自动收集候选。旧 A/B 图片与拍摄记录不删除；完整备份继续包含图片，Markdown 记录来源、文件名和借鉴点。没有新增云服务或图片打包导出。用户已认可这次更新，并明确要求更新说明文档、提交到 GitHub。真实内容的找图效果仍待实践验证。

本轮 `npm test` 三组检查已全部通过，覆盖参考任务、图片格式和大小限制、链接安全、沿用关系、旧 A/B 保留、跨刷新位置、含图片的完整备份恢复，以及原有 16 步和并行流程；已检查桌面与移动布局截图。用户已认可新版；实际搜索质量及完整创作中的效果仍待验证。

## 2026-09-22 · 第 13 步收敛试点（本地待验证）

用户希望自己负责发散，由 AI 帮助收敛，确认先在第 13 步试点。增加可选的「帮我定一个方案」入口，要求交付一张真实首选参考、可填回的借鉴点、一个眼下动作和停止标准；保留原多候选方式。没有扩展其他步骤，没有增加必填项或创建额外 Agent。网页仍整理任务，由用户复制到当前 Codex 对话发送；不直接调用搜索或生图服务。

试点刚加入时尚未执行真实找图任务；随后已进行本页下一节记录的实战，并据此调整界面。是否更容易开始拍摄或截图仍待实际使用验证。此试点随四段重构一并纳入本次获准上传的范围。

本轮三组 `npm test` 全部通过，检查试点任务四项交付、保留多候选任务，以及原有保存、备份恢复和布局。已查看页面截图；技术检查不代表实际找图或拍摄效果已经验证。

## 2026-09-22 · 实战后的半手动四段重构

在本对话中已使用 Tabbit 查看小红书原帖，生成一张 AI 构图示意，并按用户要求下载原参考、做了并排分析。用户认可流程方向，指出前期希望较充分地借鉴已有高点赞或高关注内容，再逐渐加入自己的表达。当前样例表明“过度简化批注会削弱真实使用感”；这不是发布流量验证，示例原帖也未因此被认定为同类高互动的已验证标杆。

据此把第 13 步整理为找参考、筛参考、按需生成示意、实际拍摄。页面提供手动说明和分别交给 Agent 的任务，保持干货型、反精致、有活人感的定位；每张图独立记住当前段，不强制完成所有段。高互动进入查找优先条件，数字与采集日期可记录，未知不推断；推荐仍需通过表达、风格与执行检查。

用户进一步明确关系：借鉴好点子是方法，三个内容方向是筛选依据，重点学习有效表达的核心特质，并用自己的经历、材料与判断实现。已补进页面提示、交接任务和说明；用户希望先查看现有四段，再决定后续调整，尚未确认新版界面的实际使用体验。

原参考、旧 B、AI 示意与自己的试拍使用不同保存位置，完整备份 / 恢复与 Markdown 一起扩展。已有参考及旧拍摄记录继续保留；新示意、试拍均选填，不提高第 13 步的最低完成门槛。复制的任务不能携带浏览器素材，页面提供单图下载，并明确让用户把图附到 Agent 对话。

实战用到的 Tabbit 浏览与 imagegen 已实际执行；本次只把经验落实到网页交接，不在网页接入 API，不把执行任务的按钮写成自动调用。相关图片与演练文档位于仓库外的工作区 output，本轮不会作为代码提交。

技术检查：三组 `npm test` 通过，已检查桌面与移动布局；覆盖四段切换、任务生成、原图 / 示意 / 试拍保存、旧记录兼容和完整备份恢复。新版实际填写体验、自己的实拍与发布效果仍待用户试用。用户已明确授权先按卡兹克风格改写说明，再提交 GitHub；版本与远端检查结果以 Git 历史及 Actions 为准。

本轮说明改写使用本机 `khazix-writer` Skill，从本期真实试验讲到四段用法，并将「借鉴好点子 → 三个内容方向 → 核心特质 → 自己的材料与判断」融入介绍。README 保留项目自己的叙述视角，使用说明保留可查找的操作结构。


## 2026-09-23 · 整篇一次匹配本地参考（本地修改）

用户实际使用后希望减少任务交接：第 12 步的多张配图需求，一次整理为第 13 步的一份提示词。Workbuddy 负责已有参考库的匹配与筛选，本项目只优化工作台。取消生成示意图，拿到真实参考后直接仿拍，再按需比较实拍。

当前任务明确只读本地库，不默认联网补搜；缺口需要用户另行授权。输出区分实际画面、适合用途、借鉴点，要求核实旧标签，保留每张参考与整篇需求的对应关系。实际图片和原有内容不自动修改。参考记录支持本地编号 / 路径，原链接、上传、共享参考与无需参考方式继续可用。

旧 A/B、历史示意、试拍、候选和对照文字保留，历史示意只读显示，不新增必填字段。原四段流程作为历史记录保留，不再作为当前操作说明。

验证状态：`npm test` 三组检查通过，覆盖整篇五项任务、部分填写与空白提示、多篇隔离、旧 A/B 与示意保留、完整备份恢复、Markdown 导出及并行进度；桌面与移动截图已检查，无横向溢出。尚未通过本轮提示词让 Workbuddy 实际匹配，不能把生成任务等同于筛选成功。用户已明确授权本轮提交上传；实际推送结果以 Git 记录为准。

用户反馈 Workbuddy 根据一次整篇交接找图速度快、匹配和标注基本符合需求，但五张配图各只有一张候选，比较空间不够。2026-09-23 再调整同一份提示词：每项已填配图分别给 3—5 组有差异的真实参考，每组附主图；每项制作对照页并推荐一组。本地不足就按实际数量交付和说明缺口，不扩大联网权限。页面说明与测试随之更新。本轮改动范围是工作台程序与说明，新的逐图交付尚待 Workbuddy 实际运行验证。
